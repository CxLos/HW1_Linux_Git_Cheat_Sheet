`git init` - initialize existing directory as git repository
`git clone [url]` - retrieve a remote repository from a hosted location to your local pc
`git status` - show modified files in working directory 
